import Foundation

enum NoteCopy {
    static let startTypingMessage = String(
        localized: "note.startTyping.message",
        defaultValue: "start typing...",
        comment: "Placeholder shown when the current note has no source text or is ready for input."
    )
}
