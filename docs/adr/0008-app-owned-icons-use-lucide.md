# App 自有图标使用 Lucide

Workbench、Note Library、Settings、菜单和自定义确认界面中的 App 自有图标采用高保真原型指定的 Lucide 2px stroke 风格，不用相近的 SF Symbols 替代。系统拥有的元素仍服从 iOS，例如系统键盘、系统返回手势和 ActivityKit 托管行为。该选择优先保证原型一致性，代价是需要维护矢量资产、统一尺寸与描边渲染，并自行确保 Dynamic Type、对比度和无障碍标签完整。
