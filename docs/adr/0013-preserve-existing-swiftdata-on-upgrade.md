# 升级时无损保留现有 SwiftData

新版必须在现有 IslandNotes 数据之上原地工作，保留当前便签 ID、正文、内容版本、时间、Workbench 指针和全部 Note Library 条目；不得通过删除数据库、重建容器或重新生成当前便签简化重构。旧正文直接成为新版源文本并使用新的展示规则，首次打开新版进入展示态，新引入的 Display Mode 默认为 Automatic。测试必须覆盖从现有数据启动和恢复，确保界面与架构重构不造成用户内容丢失。
